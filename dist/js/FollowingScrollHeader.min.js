(() => { let $s = null; const sai = () => { $s = document.getElementById("header") }, sa = () => { if (null === $s) return; const sy = window.scrollY, classList = $s.classList, ic = classList.contains("js_active"); !ic && 500 <= sy ? classList.add("js_active") : ic && 500 >= sy && classList.remove("js_active") }; window.addEventListener("load", sai), window.addEventListener("scroll", sa) })();